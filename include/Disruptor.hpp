#pragma once
#include "RingBuffer.hpp"

namespace disruptor {
    template<typename T, size_t N>
    class Disruptor {
        RingBuffer<T> ringBuffer;

        tiếp tục nghĩ cách tạo thread factory hoặc tạo mặc định đơn giản cho dễ

    };
}
